# slack installation

sudo ln -s /var/lib/snapd/snap /snap
sudo snap install slack --classic

# mozilla password imports

signon.management.page.fileImport.enabled -> True

# github link remote

git remote set-url origin git@gitlab.com:USERNAME/PROJECT_NAME.git

# Note on clock/dark mode issue
# 
# if at start up, the time is wrong and the internet is broken,
# go into:
#Settings -> Date & Time -> revert to automatic time
# 
# if the layout switched back to light mode, check in:
# Settings -> Layout Switcher -> disable auto dark mode
# then go into:
# Tweaks -> Appearance -> revert to a better application theme
